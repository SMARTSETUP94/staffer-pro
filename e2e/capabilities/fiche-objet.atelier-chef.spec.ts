/**
 * Lot 8.2 — Fiche Objet : matrice atelier_chef.
 *
 * STUB — nécessite un compte test atelier_chef seedé.
 * Attendu : édite respo_fab_id + commentaire ; reste (nom/quantité/heures) disabled.
 *
 * TODO(8.2.x) : seeder `E2E_ATELIER_CHEF_EMAIL` + ajouter project Playwright.
 */
import { test } from "@playwright/test";

test.describe.skip("fiche-objet :: atelier_chef (respo + commentaire)", () => {
  test("nom/heures disabled, respo_fab_id éditable", async () => {
    // À implémenter quand le compte test atelier_chef sera seedé.
  });
});
