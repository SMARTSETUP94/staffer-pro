/**
 * v0.26.0 — Wrapper widget pour le bloc Flotte KPIs existant.
 */
import { FlotteKpisBloc } from "@/components/dashboard/FlotteKpisBloc";

export function FlotteKpisWidget() {
  return <FlotteKpisBloc />;
}
