/**
 * v0.50 (L4c) — Composant legacy masqué.
 * Remplacé par AppSidebar drawer unique capability-driven (L4b).
 * Plus rendu dans aucun layout. Conservé comme no-op pour ne pas casser
 * d'éventuels imports résiduels. Sera supprimé en L4d.
 */
export function MobileBottomNav() {
  return null;
}
